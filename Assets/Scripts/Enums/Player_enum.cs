public enum PowerUps {
    DJump = 1,
    Dash = 2
}

public enum States {
    dead,
    grounded,
    airborne,
    damage,
    idle,
    walk,
    attack,
    jump,
    dash,
    fall
}

public enum PlayerHealthRange {
    LOW = 3,
    MID = 2,
    HIGH = 1
}
